#include "main.h"
/**
 * _strlen - return the length of a string
 * @s: char to check
 * Return: returns length of the length
*/

int _strlen(char *s)
{
	int len = 0;

	while(; *s++;)
	{
		len++
	}
	return (len);
}
